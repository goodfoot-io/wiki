.devcontainer/Dockerfile#L114-L117 rk64:a234c0f99b549401
.devcontainer/docker-compose.yml#L14-L21 rk64:0aac557e81cc0bd1
.devcontainer/post-create.sh#L64-L105 rk64:3a611dd38364eb00

Tailscale integration wiring the devcontainer: docker-compose supplies the .env auth vars and the host-backed /home/node bind mount that persists Tailscale state across rebuilds, the Dockerfile installs the tailscale/tailscaled binaries, and post-create.sh configures and starts the daemon in userspace-networking mode.
