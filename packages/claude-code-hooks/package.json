{
  "name": "@goodfoot/wiki-claude-code-hooks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "claude-code-hooks -i 'src/*.ts' -o '../../plugins/wiki/hooks/hooks.json' --no-sourcemap",
    "lint": "npx biome check src",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@goodfoot/claude-code-hooks": "^1.9.0",
    "@typescript/native-preview": "^7.0.0-dev",
    "typescript": "^5.9.3",
    "vitest": "^3.0.0"
  }
}
